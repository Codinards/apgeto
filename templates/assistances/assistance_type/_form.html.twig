{{ form_start(form) }}
{{ form_widget(form) }}
<div class="form-group mt-4">
    <button class="btn btn-primary">{{ __u( button_label ?? 'save') }}</button>
</div>
{{ form_end(form) }}

<script defer>
    const radios = document.querySelector('#base_assistance_isAmount').querySelectorAll('input')
    for( const radio of radios) radio.removeAttribute('required')
</script>